{
  "name": "acv-customer-web",
  "version": "0.1.0",
  "private": true,
  "description": "Customer-facing web entry point for the custom Audio & Video channel (ACS). Phase 3 scaffold.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@azure/communication-calling": "^1.31.1",
    "@azure/communication-common": "^2.3.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
