{
  "name": "acv-deployment-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "Local, static deployment assistant for the custom Audio & Video channel. Phase 3.5 scaffold — collects inputs and generates a deployment plan locally. Does NOT call Azure or Dynamics 365.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
